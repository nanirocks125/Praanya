//
//  SummaryView.swift
//  Praanya
//
//  Created by Manikanta Nandam on 24/08/25.
//

import SwiftUI
/*
struct SummaryView: View {
    @ObservedObject var viewModel: InventoryViewModel

    var body: some View {
        HStack(spacing: 20) {
            SummaryCardView(title: "Total Items",
                            value: "\(viewModel.totalInStock)",
                            description: "Total items in stock",
                            icon: "archivebox.fill",
                            color: .blue.opacity(0.2))
            SummaryCardView(title: "Low Stock Items",
                            value: "\(viewModel.lowStockCount)",
                            description: "Number of items that are running low",
                            icon: "exclamationmark.triangle.fill",
                            color: .orange.opacity(0.2))
            SummaryCardView(title: "Expired Items",
                            value: "\(viewModel.expiredCount)",
                            description: "Number of items past their expiration date",
                            icon: "xmark.octagon.fill",
                            color: .red.opacity(0.2))
            SummaryCardView(title: "Out of Stock Items",
                            value: "\(viewModel.outOfStockCount)",
                            description: "Count of items currently out of stock",
                            icon: "nosign",
                            color: .gray.opacity(0.2))
        }
    }
}
*/
